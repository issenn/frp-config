[common]
bind_port = 7000

vhost_http_port = {{ .Envs.FRP_VHOST_HTTP_PORT }}
vhost_https_port = {{ .Envs.FRP_VHOST_HTTPS_PORT }}

dashboard_addr = 0.0.0.0
dashboard_port = 7500

dashboard_user = admin
dashboard_pwd = admin

token = issenn

subdomain_host = {{ .Envs.FRP_SUBDOMAIN_HOST }}
